---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .env.local
  - .env.example
  - .gitignore
autonomous: false
requirements: [FOUND-06, FOUND-07]

must_haves:
  truths:
    - "Vercel project is linked and deploys automatically on push to main"
    - "Preview deployments are created for branches"
    - "NEXT_PUBLIC_CLOUDFLARE_URL environment variable is configured"
    - ".env.example documents the required environment variable"
  artifacts:
    - path: ".env.example"
      provides: "Documentation of required env vars"
      contains: "NEXT_PUBLIC_CLOUDFLARE_URL"
    - path: ".env.local"
      provides: "Local environment variables"
      contains: "NEXT_PUBLIC_CLOUDFLARE_URL"
  key_links:
    - from: ".env.local"
      to: "Vercel environment variables"
      via: "Same variable names"
      pattern: "NEXT_PUBLIC_CLOUDFLARE_URL"
---

<objective>
Deploy the project to Vercel with CI on main branch and configure the Cloudflare media URL environment variable. This establishes the deployment pipeline that all subsequent phases use for preview and production.

Purpose: Enable continuous deployment so every push to main is live, and configure the Cloudflare base URL for media assets used in Phase 4.
Output: Live Vercel deployment, .env.example documenting required env vars.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure environment variables and deploy to Vercel</name>
  <files>.env.local, .env.example, .gitignore</files>
  <read_first>
    - .gitignore (ensure .env.local is already gitignored by create-next-app)
    - .planning/phases/01-foundation/01-CONTEXT.md (deployment decisions: Vercel CI on main, NEXT_PUBLIC_CLOUDFLARE_URL as env var)
  </read_first>
  <action>
1. Create .env.example (committed to git, documents required env vars):
   ```
   # Cloudflare CDN base URL for media assets (photos, videos)
   # Set in Vercel dashboard: Settings > Environment Variables
   NEXT_PUBLIC_CLOUDFLARE_URL=https://your-cloudflare-subdomain.example.com
   ```

2. Create .env.local (NOT committed — local dev):
   ```
   NEXT_PUBLIC_CLOUDFLARE_URL=https://placeholder.example.com
   ```

3. Verify .gitignore includes `.env*.local` (create-next-app should have added this). If missing, add it.

4. Initialize git repository if not already initialized:
   ```
   git init
   git add -A
   git commit -m "feat: scaffold Next.js 16 foundation"
   ```

5. Create a GitHub repository and push. Use gh CLI if available:
   ```
   gh repo create vidit-portfolio --public --source=. --remote=origin --push
   ```
   If gh is not available, instruct the user to create the repo manually and push.

6. Deploy to Vercel via CLI:
   ```
   npx vercel --yes
   npx vercel --prod --yes
   ```
   This links the project and triggers the first production deployment.

7. Set the Cloudflare env var on Vercel:
   ```
   npx vercel env add NEXT_PUBLIC_CLOUDFLARE_URL production preview development
   ```
   When prompted, enter `https://placeholder.example.com` (to be updated when Vidit provides the real Cloudflare URL).

8. Verify the deployment is live by checking the Vercel URL.
  </action>
  <verify>
    <automated>npx vercel ls 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains "NEXT_PUBLIC_CLOUDFLARE_URL="
    - .env.local contains "NEXT_PUBLIC_CLOUDFLARE_URL="
    - .gitignore contains ".env*.local" or ".env.local"
    - .env.local is NOT tracked by git (git ls-files .env.local returns empty)
    - `npx vercel ls` shows at least one deployment
  </acceptance_criteria>
  <done>Vercel deployment live and auto-deploying on push to main. NEXT_PUBLIC_CLOUDFLARE_URL set in Vercel env vars. .env.example documents the required variable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Vercel deployment is live</name>
  <what-built>Vercel deployment pipeline with automatic deploys on push to main. The foundation page should be visible at the Vercel preview URL showing "Vidit Dugar" on a dark background with correct fonts.</what-built>
  <how-to-verify>
    1. Open the Vercel deployment URL shown in Task 1 output
    2. Confirm: dark (#0D0D0D) background with no white flash
    3. Confirm: "Vidit Dugar" heading in serif font (Playfair Display)
    4. Confirm: "Investor. Operator. Builder." in sans-serif (DM Sans)
    5. Confirm: "Foundation complete" in monospace (DM Mono) with gold accent color
    6. Open browser Network tab, reload, confirm NO requests to fonts.googleapis.com
    7. Push a trivial change to main and confirm Vercel auto-deploys within ~60 seconds
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
1. Vercel deployment URL is accessible and shows the foundation page
2. Auto-deploy on push to main works
3. .env.example committed to git documenting NEXT_PUBLIC_CLOUDFLARE_URL
4. .env.local exists locally but is gitignored
</verification>

<success_criteria>
- Vercel project linked and auto-deploys on push to main
- Preview deployments work on branch pushes
- NEXT_PUBLIC_CLOUDFLARE_URL configured in Vercel env vars
- .env.example documents required environment variables
- User has verified the live deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>

---
phase: 05-performance-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/hero/StatCountUp.tsx
  - components/hero/HeroAnimations.tsx
  - components/about/AboutAnimations.tsx
  - components/philosophy/PhilosophyAnimations.tsx
  - components/fund/FundAnimations.tsx
  - components/kilrr/KilrrAnimations.tsx
  - components/timeline/TimelineAnimations.tsx
  - components/beyondwork/BeyondWorkAnimations.tsx
  - components/media/MediaAnimations.tsx
  - components/media/GalleryGrid.tsx
  - components/media/LinkedInCard.tsx
  - components/sections/FooterSection.tsx
  - app/layout.tsx
  - app/robots.ts
  - app/sitemap.ts
autonomous: true
requirements: [PERF-01, PERF-04]

must_haves:
  truths:
    - "All small text (text-xs, text-sm, text-base) achieves WCAG AA 4.5:1 contrast ratio against its background"
    - "SEO metadata includes metadataBase, openGraph, twitter card fields"
    - "robots.txt is served at /robots.txt via app/robots.ts"
    - "sitemap.xml is served at /sitemap.xml via app/sitemap.ts"
    - "next build passes with zero errors after all changes"
  artifacts:
    - path: "app/robots.ts"
      provides: "robots.txt auto-served by Next.js"
      exports: ["default"]
    - path: "app/sitemap.ts"
      provides: "sitemap.xml auto-served by Next.js"
      exports: ["default"]
    - path: "app/layout.tsx"
      provides: "Complete SEO metadata with metadataBase and openGraph"
      contains: "metadataBase"
  key_links:
    - from: "app/layout.tsx"
      to: "metadataBase URL"
      via: "metadata export"
      pattern: "metadataBase.*new URL"
    - from: "app/robots.ts"
      to: "sitemap URL"
      via: "sitemap field"
      pattern: "sitemap.*vidit-portfolio"
---

<objective>
Fix WCAG AA color contrast violations on all small text and add missing SEO metadata fields to achieve Lighthouse 90+ on Accessibility and SEO categories.

Purpose: The muted (#8A7E74) and accent (#A6701A) colors fail WCAG AA 4.5:1 contrast ratio at small text sizes (text-xs, text-sm, text-base). Lighthouse Accessibility will tank without fixing these. SEO is missing metadataBase, openGraph, twitter card, robots.ts, and sitemap.ts.

Output: All 12 component files updated with accessible color classes; layout.tsx updated with full SEO metadata; robots.ts and sitemap.ts created.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-performance-and-deployment/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WCAG AA contrast violations across all components</name>
  <files>
    components/hero/StatCountUp.tsx,
    components/hero/HeroAnimations.tsx,
    components/about/AboutAnimations.tsx,
    components/philosophy/PhilosophyAnimations.tsx,
    components/fund/FundAnimations.tsx,
    components/kilrr/KilrrAnimations.tsx,
    components/timeline/TimelineAnimations.tsx,
    components/beyondwork/BeyondWorkAnimations.tsx,
    components/media/MediaAnimations.tsx,
    components/media/GalleryGrid.tsx,
    components/media/LinkedInCard.tsx,
    components/sections/FooterSection.tsx
  </files>
  <read_first>
    components/hero/StatCountUp.tsx,
    components/hero/HeroAnimations.tsx,
    components/about/AboutAnimations.tsx,
    components/philosophy/PhilosophyAnimations.tsx,
    components/fund/FundAnimations.tsx,
    components/kilrr/KilrrAnimations.tsx,
    components/timeline/TimelineAnimations.tsx,
    components/beyondwork/BeyondWorkAnimations.tsx,
    components/media/MediaAnimations.tsx,
    components/media/GalleryGrid.tsx,
    components/media/LinkedInCard.tsx,
    components/sections/FooterSection.tsx
  </read_first>
  <action>
Replace `text-muted` and `text-accent` at small text sizes with higher-contrast alternatives. The core principle: `text-foreground/70` (~5.2:1 against #F5EFE6) replaces `text-muted` for informational small text. `text-foreground/60` replaces very low-emphasis text. `text-accent` at large sizes (text-lg+) or bold is fine (passes 3:1 large text threshold). `text-accent` at text-xs must become `text-foreground/70` or darker.

IMPORTANT: Do NOT change the Tailwind theme tokens. Only change class names in component JSX.

Specific changes by file:

**StatCountUp.tsx:**
- Line with `text-xs text-muted` (stat labels): change to `text-xs text-foreground/70`

**HeroAnimations.tsx:**
- `text-sm text-muted` (subline): change to `text-sm text-foreground/70`
- `text-[11px] uppercase tracking-[0.15em] text-muted/70` (career pills): change to `text-foreground/50` and border to `border-foreground/20`
- `text-[11px] text-muted/45` (cities line): change to `text-foreground/40` — this is decorative large-letter-spaced text, acceptable at lower contrast
- `text-lg md:text-xl text-accent` (identity descriptor "Investor. Operator. Builder."): KEEP — text-lg is 18px which qualifies as WCAG large text, 3.71:1 passes the 3:1 threshold

**AboutAnimations.tsx:**
- `text-base text-muted` (prose paragraphs): change to `text-base text-foreground/75`

**PhilosophyAnimations.tsx:**
- `text-base text-muted` (pillar body text): change to `text-base text-foreground/75`
- `text-4xl text-accent/30` (pillar numbers like "01"): KEEP — decorative large text at very low opacity, WCAG incidental text exemption applies

**FundAnimations.tsx:**
- `text-sm text-muted` (subtitle): change to `text-sm text-foreground/70`
- `text-muted` on table cells (sector, entryValuation, latestValuation): change to `text-foreground/70`
- `text-xs text-muted` (co-investors): change to `text-xs text-foreground/70`
- `text-accent font-semibold` (multiple column table header and data): KEEP — font-semibold at default table size is borderline; to be safe, change the `th` with `text-accent` to `text-foreground` and keep the `td` multiple values as `text-accent font-semibold` (bold text at ~14px passes large text threshold at 3.71:1)

**KilrrAnimations.tsx:**
- `text-xs uppercase tracking-[0.15em] text-accent` (overline "Investment Story"): change to `text-xs uppercase tracking-[0.15em] text-foreground/60`
- `text-base text-muted` (body paragraphs): change to `text-base text-foreground/75`

**TimelineAnimations.tsx:**
- `text-xs text-muted` (dates): change to `text-xs text-foreground/60`
- `text-sm text-accent` (organization names): change to `text-sm text-foreground font-medium` — organization names are informational, need full contrast
- `text-sm text-muted` (descriptions): change to `text-sm text-foreground/70`

**BeyondWorkAnimations.tsx:**
- `text-sm text-muted` (both story and interests text): change to `text-sm text-foreground/70`

**MediaAnimations.tsx:**
- `text-xs uppercase tracking-[0.15em] text-accent` (section overline): change to `text-xs uppercase tracking-[0.15em] text-foreground/60`
- `text-sm text-muted italic` (placeholder text): change to `text-sm text-foreground/60 italic`
- `text-xs text-muted` (the "--" divider and "read more"): change to `text-xs text-foreground/60`

**GalleryGrid.tsx:**
- `text-xs uppercase tracking-[0.15em] text-accent` (Gallery overline): change to `text-xs uppercase tracking-[0.15em] text-foreground/60`
- `text-sm text-muted italic` (placeholder): change to `text-sm text-foreground/60 italic`

**LinkedInCard.tsx:**
- `text-xs text-muted` (date): change to `text-xs text-foreground/60`
- `text-xs text-accent hover:text-accent/80` (read more link): change to `text-xs text-foreground/70 hover:text-foreground transition-colors`

**FooterSection.tsx:**
- `text-sm text-accent hover:text-accent-hover` (email and LinkedIn links): KEEP — links are interactive, accent color at text-sm is borderline but links have underline/hover distinction. However, to be safe, change to `text-sm text-foreground/80 hover:text-foreground transition-colors`
- `text-sm text-muted` (closing line): change to `text-sm text-foreground/70`
- `text-xs text-muted/60` (copyright): change to `text-xs text-foreground/50`

After all changes, run `npm run build` to confirm zero TypeScript errors.
  </action>
  <verify>
    <automated>cd "C:\Users\Vidit\OneDrive\Desktop\Claude\Vidit Portfolio - Claude" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `text-muted` in components/ returns ZERO matches at text-xs or text-sm sizes (only allowed at text-lg+ or in Tailwind theme)
    - grep for `text-accent` in components/ returns matches ONLY at text-lg+ sizes, font-semibold contexts, or the PhilosophyAnimations decorative number
    - `npm run build` exits 0 with zero TypeScript errors
    - StatCountUp.tsx contains `text-xs text-foreground/70` for stat labels
    - HeroAnimations.tsx subline contains `text-sm text-foreground/70`
    - FooterSection.tsx closing line contains `text-sm text-foreground/70`
    - FooterSection.tsx copyright contains `text-xs text-foreground/50`
    - TimelineAnimations.tsx org names contain `text-foreground font-medium` (not text-accent)
    - KilrrAnimations.tsx body contains `text-foreground/75`
    - All overline labels (KilrrAnimations, MediaAnimations, GalleryGrid) use `text-foreground/60` not `text-accent`
  </acceptance_criteria>
  <done>Every text-muted and text-accent usage at small text sizes (text-xs, text-sm, text-base) is replaced with text-foreground at appropriate opacity levels that achieve WCAG AA 4.5:1 contrast. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEO metadata, robots.ts, and sitemap.ts</name>
  <files>
    app/layout.tsx,
    app/robots.ts,
    app/sitemap.ts
  </files>
  <read_first>
    app/layout.tsx
  </read_first>
  <action>
**app/layout.tsx** — Update the existing `metadata` export to include full SEO fields. Keep all existing imports and font config unchanged. Replace ONLY the metadata export:

```typescript
export const metadata: Metadata = {
  metadataBase: new URL('https://vidit-portfolio-vert.vercel.app'),
  title: 'Vidit Dugar — Investor. Operator. Builder.',
  description: 'Early-stage consumer investor in India. Formerly Nomura London, Bombay Shaving Company, The Barbershop Fund.',
  openGraph: {
    type: 'website',
    title: 'Vidit Dugar — Investor. Operator. Builder.',
    description: 'Early-stage consumer investor in India. Formerly Nomura London, Bombay Shaving Company, The Barbershop Fund.',
    url: 'https://vidit-portfolio-vert.vercel.app',
    siteName: 'Vidit Dugar',
  },
  twitter: {
    card: 'summary',
    title: 'Vidit Dugar — Investor. Operator. Builder.',
    description: 'Early-stage consumer investor in India. Formerly Nomura London, Bombay Shaving Company, The Barbershop Fund.',
  },
}
```

**app/robots.ts** — Create new file:

```typescript
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: 'https://vidit-portfolio-vert.vercel.app/sitemap.xml',
  }
}
```

**app/sitemap.ts** — Create new file:

```typescript
import type { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://vidit-portfolio-vert.vercel.app',
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 1,
    },
  ]
}
```

Run `npm run build` to confirm everything compiles.
  </action>
  <verify>
    <automated>cd "C:\Users\Vidit\OneDrive\Desktop\Claude\Vidit Portfolio - Claude" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains the string `metadataBase: new URL('https://vidit-portfolio-vert.vercel.app')`
    - app/layout.tsx contains `openGraph:` block with `type: 'website'`
    - app/layout.tsx contains `twitter:` block with `card: 'summary'`
    - app/robots.ts exists and contains `MetadataRoute.Robots`
    - app/robots.ts contains `sitemap: 'https://vidit-portfolio-vert.vercel.app/sitemap.xml'`
    - app/sitemap.ts exists and contains `MetadataRoute.Sitemap`
    - app/sitemap.ts contains `url: 'https://vidit-portfolio-vert.vercel.app'`
    - `npm run build` exits 0 with zero TypeScript errors
  </acceptance_criteria>
  <done>SEO metadata is complete with metadataBase, openGraph, and twitter card. robots.ts and sitemap.ts exist and will be auto-served by Next.js at /robots.txt and /sitemap.xml. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `grep -r "text-muted" components/ --include="*.tsx"` returns no matches at text-xs/text-sm sizes
3. `grep "metadataBase" app/layout.tsx` returns a match
4. `app/robots.ts` and `app/sitemap.ts` exist
5. `npm run lint` passes
</verification>

<success_criteria>
- All small-text color contrast violations fixed (text-muted and text-accent replaced with text-foreground/N at appropriate opacities)
- SEO metadata complete with metadataBase, openGraph, twitter card
- robots.ts and sitemap.ts created and properly typed
- next build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-deployment/05-01-SUMMARY.md`
</output>

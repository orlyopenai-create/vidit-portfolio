---
phase: 05-performance-and-deployment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06]

must_haves:
  truths:
    - "Lighthouse scores 90+ on Performance, Accessibility, Best Practices, and SEO on production URL"
    - "Site renders correctly at 375px viewport width with no horizontal overflow"
    - "No white flash on hard reload"
    - "All whileInView animations use viewport={{ once: true }}"
    - "All section components are Server Components — no use client in sections/"
    - "next build passes TypeScript strict with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Verify all six PERF requirements on the live production URL after contrast fixes and SEO metadata from Plan 01 are deployed.

Purpose: This is the final gate for the entire v1 milestone. The code changes are done — this plan confirms they produce the required Lighthouse scores and visual correctness on production.

Output: Verified Lighthouse scores, responsive layout confirmation, and all PERF requirements signed off.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-performance-and-deployment/05-RESEARCH.md
@.planning/phases/05-performance-and-deployment/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PERF-02, PERF-03, PERF-05, PERF-06 via static analysis</name>
  <files></files>
  <read_first>
    components/hero/HeroAnimations.tsx,
    components/hero/StatCountUp.tsx,
    components/about/AboutAnimations.tsx,
    components/fund/FundAnimations.tsx,
    components/timeline/TimelineAnimations.tsx,
    components/media/MediaAnimations.tsx,
    components/beyondwork/BeyondWorkAnimations.tsx,
    app/globals.css,
    tsconfig.json
  </read_first>
  <action>
Run the following verification commands and confirm each passes:

**PERF-02 (viewport once: true):**
Run `grep -rn "whileInView" components/ --include="*.tsx"` and verify every match has a corresponding `once: true` on the same element or in a viewport prop. There should be zero whileInView usages without `once: true`.

**PERF-03 (Server Components for sections):**
Run `grep -rn "use client" components/sections/ --include="*.tsx"` and verify it returns ZERO matches. All section wrappers must be Server Components. The `'use client'` directive should only exist in leaf animation wrappers (components/hero/, components/fund/, etc.), not in components/sections/.

**PERF-05 (No white flash):**
Read `app/globals.css` and confirm it contains `background-color: #F5EFE6` on `html, body` — this loads before JavaScript hydration, preventing any flash.

**PERF-06 (No CLS from fonts):**
Read `app/layout.tsx` and confirm all three fonts (Playfair_Display, DM_Sans, DM_Mono) use `display: 'swap'`. Read `tsconfig.json` and confirm `"strict": true` is set. Run `npm run build` and confirm zero errors.

Document each verification result. If any check fails, fix it before proceeding.
  </action>
  <verify>
    <automated>cd "C:\Users\Vidit\OneDrive\Desktop\Claude\Vidit Portfolio - Claude" && grep -rn "whileInView" components/ --include="*.tsx" | grep -v "once" | wc -l && grep -rn "use client" components/sections/ --include="*.tsx" | wc -l && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "whileInView" components/ --include="*.tsx" | grep -v "once"` returns 0 lines (all whileInView have once)
    - `grep -rn "use client" components/sections/ --include="*.tsx"` returns 0 lines (all sections are Server Components)
    - app/globals.css contains `background-color: #F5EFE6` on html or body
    - app/layout.tsx contains `display: 'swap'` for all three font declarations
    - tsconfig.json contains `"strict": true`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>PERF-02, PERF-03, PERF-05, PERF-06 all verified via static analysis and build confirmation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse audit and responsive verification on production</name>
  <what-built>
    Plan 01 fixed all WCAG AA contrast violations (text-muted/text-accent replaced with text-foreground/N at accessible opacities) and added full SEO metadata (metadataBase, openGraph, twitter, robots.ts, sitemap.ts). The changes should be auto-deployed to https://vidit-portfolio-vert.vercel.app via Vercel CI.
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm deployment:**
    Visit https://vidit-portfolio-vert.vercel.app and verify the latest changes are live (check footer copyright shows the foreground color, not muted).

    **Step 2 — Run Lighthouse (PERF-01):**
    1. Open Chrome DevTools (F12) on https://vidit-portfolio-vert.vercel.app
    2. Go to Lighthouse tab
    3. Select "Navigation" mode, "Mobile" device
    4. Check all categories: Performance, Accessibility, Best Practices, SEO
    5. Click "Analyze page load"
    6. **Expected:** All four scores are 90+
    7. If any score is below 90, note the specific failing audits

    **Step 3 — Verify responsive layout (PERF-04):**
    1. In Chrome DevTools, toggle device toolbar (Ctrl+Shift+M)
    2. Set viewport to 375x812 (iPhone SE)
    3. Scroll through the entire page
    4. **Expected:** No horizontal overflow, no broken layouts
    5. Specifically check: Hero section, logo grid (should be 3 columns), timeline (should be single column), investment table (may need horizontal scroll — acceptable)

    **Step 4 — Verify no white flash (PERF-05):**
    1. On the production URL, do a hard reload (Ctrl+Shift+R)
    2. **Expected:** Background is warm sand (#F5EFE6) immediately — no white flash

    **Step 5 — Verify SEO metadata:**
    1. Visit https://vidit-portfolio-vert.vercel.app/robots.txt
    2. **Expected:** Returns text with "User-agent: *" and "Allow: /"
    3. Visit https://vidit-portfolio-vert.vercel.app/sitemap.xml
    4. **Expected:** Returns XML with the site URL

    Report all scores and any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" with Lighthouse scores, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All four Lighthouse scores are 90+ on production URL (mobile mode)
2. 375px viewport shows correct responsive layout
3. No white flash on hard reload
4. robots.txt and sitemap.xml served correctly
5. PERF-02 through PERF-06 verified via static analysis
</verification>

<success_criteria>
- Lighthouse Performance >= 90
- Lighthouse Accessibility >= 90
- Lighthouse Best Practices >= 90
- Lighthouse SEO >= 90
- Site renders correctly at 375px with no horizontal overflow
- All six PERF requirements verified and documented
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-and-deployment/05-02-SUMMARY.md`
</output>

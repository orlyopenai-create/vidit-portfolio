---
phase: 04-media-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - components/media/LinkedInCard.tsx
  - components/media/MediaAnimations.tsx
  - components/sections/MediaSection.tsx
  - app/page.tsx
autonomous: true
requirements: [MEDIA-01, MEDIA-02, MEDIA-06]

must_haves:
  truths:
    - "Writing & Media section appears on the page between Kilrr and Timeline"
    - "LinkedIn post cards display truncated text, date, and read more link"
    - "Empty posts array renders 4 placeholder cards gracefully"
    - "Two-column editorial grid layout visible on desktop"
    - "next/image Cloudflare remotePatterns configured so gallery images won't 400"
  artifacts:
    - path: "next.config.ts"
      provides: "Cloudflare remotePatterns for next/image"
      contains: "remotePatterns"
    - path: "components/media/LinkedInCard.tsx"
      provides: "LinkedIn post card with line-clamp truncation"
      contains: "line-clamp-3"
    - path: "components/media/MediaAnimations.tsx"
      provides: "Client animation wrapper for post cards"
      contains: "use client"
    - path: "components/sections/MediaSection.tsx"
      provides: "Server component shell for media section"
      exports: ["MediaSection"]
    - path: "app/page.tsx"
      provides: "MediaSection wired between KilrrSection and TimelineSection"
      contains: "MediaSection"
  key_links:
    - from: "components/sections/MediaSection.tsx"
      to: "lib/data/media.ts"
      via: "import { linkedInPosts, photos }"
      pattern: "import.*from.*lib/data/media"
    - from: "components/media/MediaAnimations.tsx"
      to: "components/media/LinkedInCard.tsx"
      via: "renders LinkedInCard for each post"
      pattern: "LinkedInCard"
    - from: "app/page.tsx"
      to: "components/sections/MediaSection.tsx"
      via: "import and render between Kilrr and Timeline"
      pattern: "MediaSection"
---

<objective>
Build the Writing & Media section shell with LinkedIn post cards, two-column editorial grid, placeholder states, and Cloudflare next/image configuration. Wire the section into page.tsx between KilrrSection and TimelineSection.

Purpose: Establishes the media section structure and LinkedIn card sub-section so the gallery (Plan 02) can be added on top.
Output: MediaSection visible on page with LinkedIn card placeholders in a two-column grid layout.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-media-section/04-RESEARCH.md

@lib/types.ts
@lib/data/media.ts
@next.config.ts
@app/page.tsx
@components/sections/KilrrSection.tsx
@components/kilrr/KilrrAnimations.tsx

<interfaces>
<!-- Types already defined in lib/types.ts — use directly -->

From lib/types.ts:
```typescript
export interface LinkedInPost {
  id: string
  excerpt: string
  date: string
  url: string
}

export interface Photo {
  id: string
  src: string
  alt: string
  caption: string
  width: number
  height: number
}

export interface MediaData {
  linkedInPosts: LinkedInPost[]
  photos: Photo[]
}
```

From lib/data/media.ts:
```typescript
export const linkedInPosts: LinkedInPost[] = []  // empty placeholder
export const photos: Photo[] = []                 // empty placeholder
```

From app/page.tsx — current section order:
```typescript
<HeroSection />
<AboutSection />
<PhilosophySection />
<FundSection />
<KilrrSection />
<TimelineSection />    // MediaSection goes BEFORE this
<BeyondWorkSection />
<FooterSection />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next.config.ts remotePatterns and build LinkedInCard + MediaAnimations</name>
  <files>next.config.ts, components/media/LinkedInCard.tsx, components/media/MediaAnimations.tsx</files>
  <read_first>
    - next.config.ts (current empty config)
    - lib/types.ts (LinkedInPost interface)
    - lib/data/media.ts (empty arrays)
    - components/kilrr/KilrrAnimations.tsx (animation pattern reference)
    - .planning/phases/04-media-section/04-RESEARCH.md (Pattern 4: LinkedIn Card, Pattern 5: Placeholder States)
  </read_first>
  <action>
**1. Update next.config.ts** — Add `images.remotePatterns` array with two entries:
- `{ protocol: 'https', hostname: '**.r2.dev' }` (Cloudflare R2 public buckets)
- `{ protocol: 'https', hostname: '**.cloudflare.com' }` (Cloudflare CDN)

Keep the existing `NextConfig` type import. The config currently has an empty object — add the `images` key.

**2. Create `components/media/LinkedInCard.tsx`** — NO 'use client' (purely presentational server-safe component):
- Props interface: `{ post: LinkedInPost }` — import `LinkedInPost` from `@/lib/types`
- Render an `<article>` with classes: `bg-surface border border-foreground/10 p-6 flex flex-col gap-4 break-inside-avoid`
- Excerpt `<p>` with classes: `font-body text-sm text-foreground line-clamp-3 leading-relaxed`
- Bottom row: `<div className="flex items-center justify-between mt-auto">`
  - `<time className="font-mono text-xs text-muted">{post.date}</time>`
  - `<a href={post.url} target="_blank" rel="noopener noreferrer" className="font-body text-xs text-accent hover:text-accent/80 transition-colors">read more →</a>`
- Named export: `export function LinkedInCard({ post }: { post: LinkedInPost })`

**3. Create `components/media/MediaAnimations.tsx`** — 'use client' animation wrapper:
- `'use client'` at top
- Import `{ m }` from `framer-motion` (NOT `motion`)
- Import `LinkedInCard` from `@/components/media/LinkedInCard`
- Import `LinkedInPost` from `@/lib/types`
- Props: `{ posts: LinkedInPost[] }`
- Render section heading: "Writing & Media" as `<h2>` with classes `font-display text-3xl md:text-4xl font-bold text-foreground mb-4`, wrapped in `m.div` with `initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.6, ease: 'easeOut' }}`
- Add a sub-heading: "Thoughts & Writing" as `<h3>` with classes `font-body text-xs uppercase tracking-[0.15em] text-accent mb-8`
- Grid container: `<div className="columns-1 md:columns-2 gap-6">`
- When `posts.length === 0`: render 4 placeholder cards — each is a `<div className="break-inside-avoid mb-6 bg-surface border border-foreground/10 border-dashed p-6 flex flex-col gap-4">` containing a `<p className="font-body text-sm text-muted italic">LinkedIn post -- Vidit to supply</p>` and a `<div className="flex items-center justify-between mt-auto"><span className="font-mono text-xs text-muted">--</span><span className="font-body text-xs text-muted">read more →</span></div>`
- When `posts.length > 0`: map over posts rendering `<div className="break-inside-avoid mb-6" key={post.id}><LinkedInCard post={post} /></div>`
- Wrap the grid container in `m.div` with `initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, ease: 'easeOut', delay: 0.15 }}`
- Named export: `export function MediaAnimations({ posts }: { posts: LinkedInPost[] })`
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains `remotePatterns`
    - next.config.ts contains `**.r2.dev`
    - next.config.ts contains `**.cloudflare.com`
    - components/media/LinkedInCard.tsx contains `line-clamp-3`
    - components/media/LinkedInCard.tsx contains `read more`
    - components/media/LinkedInCard.tsx contains `LinkedInPost`
    - components/media/LinkedInCard.tsx does NOT contain `use client`
    - components/media/MediaAnimations.tsx contains `use client`
    - components/media/MediaAnimations.tsx contains `m.div`
    - components/media/MediaAnimations.tsx does NOT contain `motion.`
    - components/media/MediaAnimations.tsx contains `columns-1 md:columns-2`
    - components/media/MediaAnimations.tsx contains `Vidit to supply`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>next.config.ts has Cloudflare remotePatterns; LinkedInCard renders truncated post with date and link; MediaAnimations wraps cards in animated two-column grid with placeholder fallback for empty arrays; TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Create MediaSection server shell and wire into page.tsx</name>
  <files>components/sections/MediaSection.tsx, app/page.tsx</files>
  <read_first>
    - app/page.tsx (current section order — MediaSection goes between KilrrSection and TimelineSection)
    - components/sections/KilrrSection.tsx (server shell pattern reference)
    - components/media/MediaAnimations.tsx (just created in Task 1)
    - lib/data/media.ts (data imports)
  </read_first>
  <action>
**1. Create `components/sections/MediaSection.tsx`** — server component (NO 'use client'):
- Import `{ linkedInPosts, photos }` from `@/lib/data/media`
- Import `{ MediaAnimations }` from `@/components/media/MediaAnimations`
- Render: `<section id="media" className="py-24 px-6"><div className="max-w-5xl mx-auto">` wrapping `<MediaAnimations posts={linkedInPosts} />`
- Leave a comment `{/* GalleryGrid will be added in Plan 02 */}` after MediaAnimations, before closing `</div>`
- Named export: `export function MediaSection()`

**2. Update `app/page.tsx`**:
- Add import: `import { MediaSection } from '@/components/sections/MediaSection'`
- Insert `<MediaSection />` between `<KilrrSection />` and `<TimelineSection />` in the JSX — the section order becomes:
  ```
  <KilrrSection />
  <MediaSection />
  <TimelineSection />
  ```
- Do NOT change any other imports or sections.
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/MediaSection.tsx does NOT contain `use client`
    - components/sections/MediaSection.tsx contains `import { linkedInPosts, photos } from`
    - components/sections/MediaSection.tsx contains `MediaAnimations`
    - components/sections/MediaSection.tsx contains `id="media"`
    - app/page.tsx contains `import { MediaSection }`
    - app/page.tsx contains `<MediaSection />` appearing after `<KilrrSection />` and before `<TimelineSection />`
    - npm run build exits 0
  </acceptance_criteria>
  <done>MediaSection server shell renders LinkedInCard sub-section via MediaAnimations; page.tsx shows MediaSection between Kilrr and Timeline; full build passes</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. `components/media/LinkedInCard.tsx` exists with `line-clamp-3` and no `use client`
4. `components/media/MediaAnimations.tsx` exists with `use client`, `m.div`, `columns-1 md:columns-2`
5. `components/sections/MediaSection.tsx` exists as server component importing from `lib/data/media`
6. `app/page.tsx` renders MediaSection between KilrrSection and TimelineSection
7. `next.config.ts` contains `remotePatterns` with `**.r2.dev` and `**.cloudflare.com`
</verification>

<success_criteria>
- The Writing & Media section is visible on the page between Kilrr Investment Story and Career Timeline
- LinkedIn post cards render with truncated text (line-clamp-3), date, and "read more" link
- Empty posts array shows 4 graceful placeholder cards (not a crash, not blank space)
- Two-column editorial grid layout displays correctly on desktop (columns-2)
- Cloudflare remotePatterns are configured for next/image (no 400 errors when gallery photos arrive)
- Full build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-section/04-01-SUMMARY.md`
</output>

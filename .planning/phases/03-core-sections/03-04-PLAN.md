---
phase: 03-core-sections
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - components/sections/BeyondWorkSection.tsx
  - components/sections/FooterSection.tsx
  - components/beyondwork/BeyondWorkAnimations.tsx
  - app/page.tsx
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, FOOT-01, FOOT-02, FOOT-03, FOOT-04]

must_haves:
  truths:
    - "Beyond Work section has two sub-sections: Story of My Life narrative and Interests"
    - "Story of My Life paragraph mentions 5 editions, 32 speakers, 500+ attendees, ~600K YouTube views"
    - "Interests displayed as inline text with middot separators, not bullets"
    - "Footer shows email as a mailto link"
    - "Footer shows LinkedIn opening in a new tab"
    - "Footer displays closing line about early-stage consumer investing"
    - "Footer shows copyright '2026 Vidit Dugar'"
    - "No contact form is present anywhere"
    - "page.tsx imports all 8 sections in scroll order"
  artifacts:
    - path: "components/sections/BeyondWorkSection.tsx"
      provides: "Beyond Work server component"
      contains: "BeyondWorkAnimations"
    - path: "components/beyondwork/BeyondWorkAnimations.tsx"
      provides: "Client wrapper for Beyond Work"
      contains: "'use client'"
    - path: "components/sections/FooterSection.tsx"
      provides: "Footer server component"
      contains: "dugarvidit@gmail.com"
    - path: "app/page.tsx"
      provides: "Main page with all 8 sections"
      contains: "FooterSection"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/"
      via: "import all 8 section components"
      pattern: "import.*Section.*from.*components/sections"
    - from: "components/sections/FooterSection.tsx"
      to: "mailto:dugarvidit@gmail.com"
      via: "href attribute"
      pattern: "mailto:dugarvidit@gmail.com"
---

<objective>
Build the Beyond Work and Footer sections, then wire ALL 8 sections into page.tsx in scroll order.

Purpose: Beyond Work humanizes Vidit for a people-business audience. Footer provides clean contact. The final wiring makes the full site scrollable top-to-bottom.
Output: Two new sections + page.tsx updated with all 8 section imports in scroll order.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-sections/03-RESEARCH.md
@vidit_dugar_portfolio_PRD.md

<interfaces>
Tailwind tokens:
- `bg-background` (#F5EFE6), `bg-surface` (#EDE4D8)
- `text-foreground` (#241E18), `text-muted` (#8A7E74), `text-accent` (#A6701A)
- `font-display`, `font-body`, `font-mono`

Section components expected to exist after Plans 01-03:
```typescript
// All in components/sections/
export function HeroSection(): JSX.Element    // Phase 2
export function AboutSection(): JSX.Element   // Plan 01
export function PhilosophySection(): JSX.Element // Plan 01
export function FundSection(): JSX.Element     // Plan 02
export function KilrrSection(): JSX.Element    // Plan 03
export function TimelineSection(): JSX.Element // Plan 03
// This plan creates:
export function BeyondWorkSection(): JSX.Element
export function FooterSection(): JSX.Element
```

Current app/page.tsx:
```tsx
import { HeroSection } from '@/components/sections/HeroSection'
export default function Home() {
  return (
    <main>
      <HeroSection />
    </main>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Beyond Work and Footer sections</name>
  <files>
    components/sections/BeyondWorkSection.tsx,
    components/beyondwork/BeyondWorkAnimations.tsx,
    components/sections/FooterSection.tsx
  </files>
  <read_first>
    app/page.tsx,
    components/hero/HeroAnimations.tsx,
    app/globals.css,
    vidit_dugar_portfolio_PRD.md,
    .planning/phases/03-core-sections/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Create `components/beyondwork/BeyondWorkAnimations.tsx`** — a `'use client'` component. Receives `storyOfMyLife` (string) and `interests` (string) as props. Layout: two sub-sections side by side on desktop (`md:grid-cols-2`), stacked on mobile.

```tsx
'use client'
import { m } from 'framer-motion'

interface BeyondWorkAnimationsProps {
  storyOfMyLife: string
  interests: string
}

export function BeyondWorkAnimations({ storyOfMyLife, interests }: BeyondWorkAnimationsProps) {
  return (
    <div>
      <m.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.6, ease: 'easeOut' }}>
        <h2 className="font-display text-3xl md:text-4xl font-bold text-foreground mb-12">Beyond Work</h2>
      </m.div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-12">
        {/* Story of My Life */}
        <m.div initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, ease: 'easeOut', delay: 0.1 }}>
          <h3 className="font-display text-xl font-bold text-foreground mb-4">Story of My Life</h3>
          <p className="font-body text-sm text-muted leading-relaxed">{storyOfMyLife}</p>
        </m.div>

        {/* Interests */}
        <m.div initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, ease: 'easeOut', delay: 0.15 }}>
          <h3 className="font-display text-xl font-bold text-foreground mb-4">Interests</h3>
          <p className="font-body text-sm text-muted leading-relaxed">{interests}</p>
        </m.div>
      </div>
    </div>
  )
}
```

**Step 2: Create `components/sections/BeyondWorkSection.tsx`** — SERVER component:

```tsx
import { BeyondWorkAnimations } from '@/components/beyondwork/BeyondWorkAnimations'

const storyOfMyLife = `In 2019, while still a student, I co-founded Story of My Life — a live storytelling event featuring entrepreneurs, activists, and public figures sharing personal turning points. Across 5 editions, we hosted 32 speakers, drew 500+ attendees, and accumulated ~600K YouTube views. It was my first taste of building something from zero — and the first time I realised that the most compelling stories are almost never the ones that begin with success.`

const interests = 'Badminton · Vipassana Meditation · Formula 1 · Bachata · Techno & House Music · Travel (30+ countries) · Board Games'

export function BeyondWorkSection() {
  return (
    <section id="beyond-work" className="py-24 px-6">
      <div className="max-w-3xl mx-auto">
        <BeyondWorkAnimations storyOfMyLife={storyOfMyLife} interests={interests} />
      </div>
    </section>
  )
}
```

CRITICAL: The `interests` string uses middot separators (`·`) — NOT bullet points. This is inline text, not a list.

**Step 3: Create `components/sections/FooterSection.tsx`** — SERVER component. The footer uses `bg-surface` for visual differentiation from the main content. Contains:
- Name: Vidit Dugar
- Email: mailto link to `dugarvidit@gmail.com`
- LinkedIn: opens in new tab
- Closing line
- Copyright
- NO contact form

```tsx
export function FooterSection() {
  return (
    <footer id="contact" className="bg-surface py-16 px-6">
      <div className="max-w-3xl mx-auto text-center">
        <h2 className="font-display text-2xl font-bold text-foreground mb-4">Vidit Dugar</h2>

        <div className="flex items-center justify-center gap-6 mb-6">
          <a
            href="mailto:dugarvidit@gmail.com"
            className="font-body text-sm text-accent hover:text-accent-hover transition-colors"
          >
            dugarvidit@gmail.com
          </a>
          <a
            href="https://www.linkedin.com/in/viditdugar"
            target="_blank"
            rel="noopener noreferrer"
            className="font-body text-sm text-accent hover:text-accent-hover transition-colors"
          >
            LinkedIn
          </a>
        </div>

        <p className="font-body text-sm text-muted mb-8">
          Open to conversations about early-stage consumer investing in India.
        </p>

        <p className="font-body text-xs text-muted/60">
          &copy; 2026 Vidit Dugar
        </p>
      </div>
    </footer>
  )
}
```

Key details:
- `bg-surface` (`#EDE4D8`) — subtle differentiation from main `bg-background`
- Email: `<a href="mailto:dugarvidit@gmail.com">` — mailto link, NOT a form
- LinkedIn: `target="_blank" rel="noopener noreferrer"` — opens in new tab
- Closing line exact text: "Open to conversations about early-stage consumer investing in India."
- Copyright: "&copy; 2026 Vidit Dugar"
- NO `<form>` element anywhere — email link only (FOOT-04)
- NO `'use client'` — this is a server component. No animations needed for footer.
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/BeyondWorkSection.tsx does NOT contain "'use client'"
    - components/sections/BeyondWorkSection.tsx contains "5 editions"
    - components/sections/BeyondWorkSection.tsx contains "32 speakers"
    - components/sections/BeyondWorkSection.tsx contains "500+ attendees"
    - components/sections/BeyondWorkSection.tsx contains "~600K YouTube views"
    - components/sections/BeyondWorkSection.tsx contains "Badminton · Vipassana Meditation · Formula 1 · Bachata · Techno & House Music · Travel (30+ countries) · Board Games"
    - components/beyondwork/BeyondWorkAnimations.tsx contains "'use client'"
    - components/beyondwork/BeyondWorkAnimations.tsx contains "grid-cols-1 md:grid-cols-2"
    - components/beyondwork/BeyondWorkAnimations.tsx contains "whileInView" and "viewport={{ once: true }}"
    - components/beyondwork/BeyondWorkAnimations.tsx contains "m.div" and does NOT contain "motion.div"
    - components/sections/FooterSection.tsx does NOT contain "'use client'"
    - components/sections/FooterSection.tsx contains "mailto:dugarvidit@gmail.com"
    - components/sections/FooterSection.tsx contains 'target="_blank"'
    - components/sections/FooterSection.tsx contains 'rel="noopener noreferrer"'
    - components/sections/FooterSection.tsx contains "linkedin.com/in/viditdugar"
    - components/sections/FooterSection.tsx contains "Open to conversations about early-stage consumer investing in India."
    - components/sections/FooterSection.tsx contains "2026 Vidit Dugar"
    - components/sections/FooterSection.tsx contains "bg-surface"
    - components/sections/FooterSection.tsx does NOT contain "<form"
  </acceptance_criteria>
  <done>Beyond Work section renders Story of My Life and Interests side by side on desktop, stacked on mobile. Footer shows email mailto, LinkedIn new tab, closing line, copyright, and no contact form. Footer uses bg-surface background.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 8 sections into page.tsx in scroll order</name>
  <files>app/page.tsx</files>
  <read_first>
    app/page.tsx,
    components/sections/HeroSection.tsx
  </read_first>
  <action>
Replace `app/page.tsx` with the full 8-section layout. This file must remain a SERVER component (no `'use client'`). The import order and render order must match the scroll order specified in the PRD and ROADMAP.

```tsx
import { HeroSection } from '@/components/sections/HeroSection'
import { AboutSection } from '@/components/sections/AboutSection'
import { PhilosophySection } from '@/components/sections/PhilosophySection'
import { FundSection } from '@/components/sections/FundSection'
import { KilrrSection } from '@/components/sections/KilrrSection'
import { TimelineSection } from '@/components/sections/TimelineSection'
import { BeyondWorkSection } from '@/components/sections/BeyondWorkSection'
import { FooterSection } from '@/components/sections/FooterSection'

export default function Home() {
  return (
    <main>
      <HeroSection />
      <AboutSection />
      <PhilosophySection />
      <FundSection />
      <KilrrSection />
      <TimelineSection />
      <BeyondWorkSection />
      <FooterSection />
    </main>
  )
}
```

This file should be ~20 lines — clean imports and clean render. No logic, no state, no `'use client'`. Each section is a self-contained component.
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx does NOT contain "'use client'"
    - app/page.tsx contains "import { HeroSection }" (1st)
    - app/page.tsx contains "import { AboutSection }" (2nd)
    - app/page.tsx contains "import { PhilosophySection }" (3rd)
    - app/page.tsx contains "import { FundSection }" (4th)
    - app/page.tsx contains "import { KilrrSection }" (5th)
    - app/page.tsx contains "import { TimelineSection }" (6th)
    - app/page.tsx contains "import { BeyondWorkSection }" (7th)
    - app/page.tsx contains "import { FooterSection }" (8th)
    - app/page.tsx render order: HeroSection, AboutSection, PhilosophySection, FundSection, KilrrSection, TimelineSection, BeyondWorkSection, FooterSection (in that order inside <main>)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>page.tsx imports and renders all 8 sections in correct scroll order. Full site is scrollable top to bottom. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- `app/page.tsx` is under 25 lines
- All 8 sections render in correct scroll order
- No `'use client'` in page.tsx or server component sections
- Footer has mailto link and LinkedIn new tab — no contact form
- Beyond Work has Story of My Life + interests with middot separators
</verification>

<success_criteria>
- Full site scrolls: Hero → About → Philosophy → Fund → Kilrr → Timeline → Beyond Work → Footer
- Beyond Work shows Story of My Life narrative and interests inline with middot separators
- Footer shows email as mailto, LinkedIn as new tab, closing line, copyright
- No contact form present
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-sections/03-04-SUMMARY.md`
</output>

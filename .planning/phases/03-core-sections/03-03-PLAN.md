---
phase: 03-core-sections
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/timeline.ts
  - components/sections/KilrrSection.tsx
  - components/kilrr/KilrrAnimations.tsx
  - components/sections/TimelineSection.tsx
  - components/timeline/TimelineAnimations.tsx
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, TIME-01, TIME-02, TIME-03, TIME-04, TIME-05]

must_haves:
  truths:
    - "Kilrr section is labelled 'Investment Story' and contains ~300 words of case study"
    - "Kilrr section has visual differentiation from surrounding sections (bg-surface background)"
    - "Kilrr copy uses 'we/the fund' framing — no 'I' for investment decisions"
    - "Timeline shows 5 entries in reverse chronological order with full description copy"
    - "Timeline has right-aligned dates and left-aligned role/description on desktop"
    - "Timeline collapses to single column on mobile"
    - "Each timeline entry reveals with scroll-triggered animation"
  artifacts:
    - path: "components/sections/KilrrSection.tsx"
      provides: "Kilrr case study server component"
      contains: "bg-surface"
    - path: "components/kilrr/KilrrAnimations.tsx"
      provides: "Client wrapper for Kilrr fade-in"
      contains: "'use client'"
    - path: "components/sections/TimelineSection.tsx"
      provides: "Timeline server component"
      contains: "TimelineAnimations"
    - path: "components/timeline/TimelineAnimations.tsx"
      provides: "Client wrapper for staggered timeline entries"
      contains: "'use client'"
    - path: "lib/data/timeline.ts"
      provides: "Real timeline descriptions from PRD"
      contains: "Joined the family"
  key_links:
    - from: "components/sections/TimelineSection.tsx"
      to: "lib/data/timeline.ts"
      via: "import { timelineEntries }"
      pattern: "import.*timelineEntries.*from.*lib/data/timeline"
    - from: "components/timeline/TimelineAnimations.tsx"
      to: "framer-motion"
      via: "m.div whileInView per entry"
      pattern: "whileInView.*once.*true"
---

<objective>
Build the Kilrr Investment Story case study and Career Timeline sections, with all real copy from the PRD populated into the timeline data file.

Purpose: Kilrr demonstrates investment judgment through narrative. The timeline establishes professional credibility through progression. Together they form the "proof" layer of the site.
Output: Two server-component sections with client animation wrappers, timeline data file populated with real descriptions.
</objective>

<execution_context>
@C:/Users/Vidit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Vidit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-sections/03-RESEARCH.md
@vidit_dugar_portfolio_PRD.md

<interfaces>
From lib/types.ts:
```typescript
export interface TimelineEntry {
  role: string
  organization: string
  period: string
  description: string
}
```

Tailwind tokens:
- `bg-background` (#F5EFE6), `bg-surface` (#EDE4D8)
- `text-foreground` (#241E18), `text-muted` (#8A7E74), `text-accent` (#A6701A)
- `font-display`, `font-body`, `font-mono`

Animation pattern (below-fold — use whileInView, NOT animate):
```typescript
import { m } from 'framer-motion'
<m.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.6, ease: 'easeOut' }}>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Kilrr Investment Story section + populate timeline data</name>
  <files>
    lib/data/timeline.ts,
    components/sections/KilrrSection.tsx,
    components/kilrr/KilrrAnimations.tsx
  </files>
  <read_first>
    lib/data/timeline.ts,
    lib/types.ts,
    components/hero/HeroAnimations.tsx,
    vidit_dugar_portfolio_PRD.md,
    app/globals.css,
    .planning/phases/03-core-sections/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Update `lib/data/timeline.ts`** — replace ALL 5 placeholder descriptions AND fix incorrect role titles. The PRD has different role titles than the current placeholders. Here is the exact replacement data:

Entry 1 — Orly:
- `role`: `'Head of Business'` (currently says 'Co-founder & CEO' — PRD says 'Head of Business')
- `organization`: `'Orly'`
- `period`: `'Jul 2025 – Present'`
- `description`: `'Joined the family men\'s ethnic wear business to modernise operations from the ground up. Launched a D2C fulfilment channel, added tailoring as a new revenue vertical, and built data-driven inventory systems. Drove 40% average YoY monthly revenue growth; expanded cross-border into UAE and Bangladesh with 3 wholesale accounts; grew Google rating from 3.5 → 4.6 stars.'`

Entry 2 — BSC/Barbershop Fund:
- `role`: `'Chief of Staff & Head of Investments'` (currently says 'Founder & Fund Manager' — per RESEARCH open question 4, PRD says this)
- `organization`: `'Bombay Shaving Company / The Barbershop Fund'`
- `period`: `'Nov 2023 – Jul 2025'`
- `description`: `'Three-pillar role reporting directly to CEO Shantanu Deshpande.\n\nVenture Investing: Lead investment professional on a ₹25Cr Cat-I AIF with 80 LPs. Sourced, evaluated and closed 16 investments; fund at 1.8x MOIC on ₹20Cr deployed; 50%+ portfolio companies raised follow-on rounds from marquee investors.\n\nContent & Monetisation: Headed The Barbershop with Shantanu — grew YouTube from 242K to 400K+ and Instagram from 180K to 400K+, accumulating 100M+ total views; drove ₹1Cr+ in brand integration revenue.\n\nStrategy & Operations: Board presentations, D2C logistics optimisation, Trimmers category strategy, brand collaborations, hospitality vertical development.'`

Entry 3 — Nomura London:
- `role`: `'Investment Banking Associate — EMEA M&A'`  (currently says 'Analyst' — PRD says Associate)
- `organization`: `'Nomura International, London'`
- `period`: `'Jul 2021 – Sep 2023'`
- `description`: `'Advised corporates and PE investors on strategic exits, competitive sale processes and public market transactions across the UK and Europe. Played a key role in establishing Nomura\'s Secondary Advisory platform. Rating: 5/5 (Highest).\n\nSelect transactions: Sale of Vrumona (2nd largest Dutch soft drinks co.) by Heineken to Royal Unibrew | €300M (2023); Acquisition of 7IM (~£21Bn AUM) by OTPP | ~£450M (2023); Acquisition of 50% stake in Hornsea 2 (World\'s largest offshore windfarm) | £6Bn | Global M&A Deal of the Year 2022.'`

Entry 4 — Nomura Mumbai:
- `role`: `'Investment Banking Analyst — CEEMEA & Corporate DCM'` (more specific than current 'Analyst')
- `organization`: `'Nomura, Mumbai'`
- `period`: `'May 2019 – Jun 2021'`
- `description`: `'Executed >€25Bn in total new issuance across 15 issuers and 5 currencies. One of 3 analysts selected (out of ~50) for front office transfer to London. Rating: 5/5 (Highest).'`

Entry 5 — NMIMS:
- `role`: `'BSc Economics, Finance Specialisation'` (currently says 'BBA')
- `organization`: `'NMIMS School of Economics, Mumbai'`
- `period`: `'2016 – 2019'`
- `description`: `'GPA 3.7/4.0 · Dean\'s List · President, Debate & Quiz Society.'`

**Step 2: Create `components/kilrr/KilrrAnimations.tsx`** — a `'use client'` component that wraps the Kilrr case study content with fade-in animation. Receives `title` (string) and `paragraphs` (string[]) props.

```tsx
'use client'
import { m } from 'framer-motion'

interface KilrrAnimationsProps {
  title: string
  paragraphs: string[]
}

export function KilrrAnimations({ title, paragraphs }: KilrrAnimationsProps) {
  return (
    <div>
      <m.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.6, ease: 'easeOut' }}>
        <p className="font-body text-xs uppercase tracking-[0.15em] text-accent mb-4">Investment Story</p>
        <h2 className="font-display text-2xl md:text-3xl font-bold text-foreground mb-8">{title}</h2>
      </m.div>

      {paragraphs.map((p, i) => (
        <m.p key={i} initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, ease: 'easeOut', delay: i * 0.06 }} className="font-body text-base text-muted leading-relaxed mb-6">
          {p}
        </m.p>
      ))}
    </div>
  )
}
```

**Step 3: Create `components/sections/KilrrSection.tsx`** — SERVER component with `bg-surface` background for visual differentiation (CASE-02). Contains the FULL Kilrr case study copy from PRD Section 4.5. ALL investment language uses "we/the fund" — NEVER "I".

```tsx
import { KilrrAnimations } from '@/components/kilrr/KilrrAnimations'

const kilrrTitle = 'Kilrr — Backing a Founder Before the Category Existed'

const kilrrParagraphs = [
  `When Hitesh Bhagia first showed us Kilrr, the idea was simple: instant meat marinades for the Indian home cook. Clean ingredients, bold branding, 10 SKUs. No grocery store presence. No institutional backing.`,

  `Most investors would have passed. Meat spice blends felt like a niche within a niche — and the obvious question was: if there's a real market here, why haven't MDH or Everest entered it? But that question was actually the answer. The big incumbents hadn't entered because they're built for mass-market vegetarian spice blends. The meat cooking moment in India is structurally different — it's more occasion-led, more emotional, more willing to pay for convenience. Hitesh understood that instinctively. He wasn't pitching a market. He was describing his own kitchen.`,

  `What made us back him wasn't the TAM slide. It was three things: first, he was a repeat founder — his previous startup HomeVeda had been backed by Blume, and he'd learned from the experience. Second, he had the marketing instinct to make Kilrr feel like a brand, not just a product — the packaging, tone of voice, and community-building were all deliberate and all his. Third, he had the network to raise. Within months of our investment at ₹17Cr, he had competing term sheets from multiple VCs.`,

  `We entered at ₹17Cr. Kilrr is now valued at ~₹85Cr — roughly 5x — is live on Blinkit and Instamart across 190+ cities, and Hitesh recently closed a round on Shark Tank India from Anupam Mittal.`,

  `The lesson we take from Kilrr: the best early-stage bets are often hiding in plain sight. You find them by listening to founders who know their customer better than anyone in the room.`,
]

export function KilrrSection() {
  return (
    <section id="kilrr" className="bg-surface py-24 px-6">
      <div className="max-w-3xl mx-auto">
        <KilrrAnimations title={kilrrTitle} paragraphs={kilrrParagraphs} />
      </div>
    </section>
  )
}
```

CRITICAL: Verify the copy uses "we" and "us" — not "I" or "my" — for investment decisions. The PRD copy already uses "we/us/our" which is correct. Do NOT change this framing.
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/data/timeline.ts contains "Joined the family men" (Orly real description)
    - lib/data/timeline.ts contains "Three-pillar role reporting directly" (BSC real description)
    - lib/data/timeline.ts contains "Advised corporates and PE investors" (Nomura London real)
    - lib/data/timeline.ts contains "Executed >€25Bn" (Nomura Mumbai real)
    - lib/data/timeline.ts contains "GPA 3.7/4.0" (NMIMS real)
    - lib/data/timeline.ts does NOT contain "[Placeholder"
    - lib/data/timeline.ts contains "role: 'Head of Business'" (NOT 'Co-founder & CEO')
    - lib/data/timeline.ts contains "role: 'Chief of Staff & Head of Investments'" (NOT 'Founder & Fund Manager')
    - lib/data/timeline.ts contains "role: 'Investment Banking Associate" (NOT just 'Analyst' for London)
    - components/sections/KilrrSection.tsx does NOT contain "'use client'"
    - components/sections/KilrrSection.tsx contains "bg-surface"
    - components/sections/KilrrSection.tsx contains "Investment Story" or "KilrrAnimations"
    - components/sections/KilrrSection.tsx contains "Backing a Founder Before the Category Existed"
    - components/sections/KilrrSection.tsx contains "What made us back him"
    - components/sections/KilrrSection.tsx does NOT match regex "(\bI\b) (backed|invested|entered|closed)" (no first-person investment decisions)
    - components/kilrr/KilrrAnimations.tsx contains "'use client'"
    - components/kilrr/KilrrAnimations.tsx contains "whileInView" and "viewport={{ once: true }}"
    - components/kilrr/KilrrAnimations.tsx contains "m.div" or "m.p"
  </acceptance_criteria>
  <done>Kilrr section renders full ~300-word case study with "Investment Story" label, bg-surface background, and "we/the fund" framing. Timeline data file has all 5 real descriptions and corrected role titles.</done>
</task>

<task type="auto">
  <name>Task 2: Build Career Timeline section with responsive two-column layout</name>
  <files>
    components/sections/TimelineSection.tsx,
    components/timeline/TimelineAnimations.tsx
  </files>
  <read_first>
    lib/data/timeline.ts,
    lib/types.ts,
    components/hero/HeroAnimations.tsx,
    components/kilrr/KilrrAnimations.tsx,
    app/globals.css,
    .planning/phases/03-core-sections/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Create `components/timeline/TimelineAnimations.tsx`** — a `'use client'` component. Receives `entries: TimelineEntry[]`. Each entry renders in a two-column grid (date left, content right) on desktop, single column on mobile. Each entry fades in with staggered delay on scroll.

```tsx
'use client'
import { m } from 'framer-motion'
import type { TimelineEntry } from '@/lib/types'

export function TimelineAnimations({ entries }: { entries: TimelineEntry[] }) {
  return (
    <div>
      <m.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.6, ease: 'easeOut' }}>
        <h2 className="font-display text-3xl md:text-4xl font-bold text-foreground mb-12">Career</h2>
      </m.div>

      <div className="space-y-0">
        {entries.map((entry, i) => (
          <m.div
            key={entry.organization}
            initial={{ opacity: 0, y: 12 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.5, ease: 'easeOut', delay: i * 0.08 }}
            className="grid grid-cols-1 md:grid-cols-[160px_1fr] gap-2 md:gap-8 py-6 border-b border-muted/15"
          >
            <div className="font-mono text-xs text-muted md:text-right pt-1">
              {entry.period}
            </div>
            <div>
              <p className="font-display text-lg font-bold text-foreground">{entry.role}</p>
              <p className="font-body text-sm text-accent mb-2">{entry.organization}</p>
              <p className="font-body text-sm text-muted leading-relaxed whitespace-pre-line">{entry.description}</p>
            </div>
          </m.div>
        ))}
      </div>
    </div>
  )
}
```

Key layout details:
- Grid: `grid-cols-1 md:grid-cols-[160px_1fr]` — single column on mobile, two columns on desktop
- Date column: `font-mono text-xs text-muted md:text-right` — right-aligned on desktop, left on mobile
- Role: `font-display text-lg font-bold text-foreground` — Playfair Display
- Organization: `font-body text-sm text-accent` — gold accent color
- Description: `font-body text-sm text-muted leading-relaxed whitespace-pre-line` — preserves `\n\n` paragraph breaks in longer entries (BSC, Nomura London)
- Each entry separated by `border-b border-muted/15`
- Stagger: `delay: i * 0.08` per entry
- Uses `m.div` (NOT `motion.div`)

**Step 2: Create `components/sections/TimelineSection.tsx`** — SERVER component:

```tsx
import { timelineEntries } from '@/lib/data/timeline'
import { TimelineAnimations } from '@/components/timeline/TimelineAnimations'

export function TimelineSection() {
  return (
    <section id="timeline" className="py-24 px-6">
      <div className="max-w-3xl mx-auto">
        <TimelineAnimations entries={timelineEntries} />
      </div>
    </section>
  )
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/Vidit/OneDrive/Desktop/Claude/Vidit Portfolio - Claude" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/TimelineSection.tsx does NOT contain "'use client'"
    - components/sections/TimelineSection.tsx contains "import { timelineEntries } from '@/lib/data/timeline'"
    - components/sections/TimelineSection.tsx contains "TimelineAnimations"
    - components/timeline/TimelineAnimations.tsx contains "'use client'"
    - components/timeline/TimelineAnimations.tsx contains "grid-cols-1 md:grid-cols-[160px_1fr]"
    - components/timeline/TimelineAnimations.tsx contains "whileInView" and "viewport={{ once: true }}"
    - components/timeline/TimelineAnimations.tsx contains "whitespace-pre-line"
    - components/timeline/TimelineAnimations.tsx contains "font-mono text-xs text-muted md:text-right"
    - components/timeline/TimelineAnimations.tsx contains "m.div" and does NOT contain "motion.div"
    - components/timeline/TimelineAnimations.tsx contains "delay: i * 0.08"
    - components/timeline/TimelineAnimations.tsx contains "border-b border-muted/15"
  </acceptance_criteria>
  <done>Timeline section renders 5 entries in reverse chronological order with two-column layout (date left, content right) on desktop, single column on mobile. Each entry fades in with staggered scroll animation. Full descriptions with preserved paragraph breaks.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes successfully
- Timeline data file has no placeholder text
- Timeline role titles match PRD (not original scaffolded values)
- Kilrr section uses "we/the fund" framing — no "I" for investment decisions
- Kilrr section has `bg-surface` background differentiation
- Timeline grid collapses to single column on mobile (`grid-cols-1 md:grid-cols-[160px_1fr]`)
</verification>

<success_criteria>
- Kilrr case study renders ~300 words with "Investment Story" label and bg-surface background
- Career timeline shows 5 entries: Orly, BSC/Barbershop Fund, Nomura London, Nomura Mumbai, NMIMS
- Timeline dates right-aligned on desktop, single column on mobile
- Each timeline entry reveals on scroll with stagger
- All copy matches PRD verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-sections/03-03-SUMMARY.md`
</output>
